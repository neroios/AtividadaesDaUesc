#include <stdio.h>

int main() {
    printf("Hello world!\n"); // n quebra linha
    printf("\tHello world!"); // t coloca em bloco?
    // nao faco ideia do que tem que fazer aqui, so to usando o que vi no slide
    //%d para inteiro
    //%f para numero com virgula
    //%lf para numeros com virgula maiores
    //%s definindo a variavel com a quantia de [char:x] para string

    return 0;
}